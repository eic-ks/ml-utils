[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "flops_profiler"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["torch>=2.0"]